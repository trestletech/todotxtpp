#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../todo.txt-node/bin/todo" "$@"
  ret=$?
else 
  node  "$basedir/../todo.txt-node/bin/todo" "$@"
  ret=$?
fi
exit $ret
